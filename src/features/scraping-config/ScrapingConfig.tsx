const ScrapingConfig = () => {
  return (
    <div className="feature-container">
      <h1>Configurar Scraping</h1>
      {/* Aquí irá el contenido específico de configuración de scraping */}
    </div>
  );
};

export default ScrapingConfig;